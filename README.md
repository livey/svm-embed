svm-embed
=========

support vector machine for embedded system

The commanline -f data 
Here data stands for the datasets file. In the later version,some more features will be added in.

Data sets can be any name,but the data for is specified as below.

Label:value1:value2:....:valueN 

each line ended up with a 'enter' symbol, except the last line.

